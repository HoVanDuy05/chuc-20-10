import React from "react";
import { Layout } from "antd";

const { Footer: AntFooter } = Layout;

const Footer: React.FC = () => (
  <AntFooter className="footer">
    💕 Với tất cả yêu thương — Happy Vietnamese Women’s Day 20/10 💕
  </AntFooter>
);

export default Footer;
